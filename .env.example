# Copy to .env and fill in. Both values come from https://developer.adzuna.com/
# dashboard after signup — the App ID is the short one, the Key is 32 hex chars.
ADZUNA_APP_ID=your_app_id
ADZUNA_APP_KEY=your_app_key
